# Модуль `billing`

**Статус:** **реализован (Sprint 3.5)**; **прод-фикс контракта вебхука [ADR-040](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md) зафиксирован в docs — требует доработки кода** (ключ дедупа `event_properties.profile_event_id`→synthetic, запрет тихого дропа, `KNOWN_EVENT_TYPES`=18 фактических типов, `subscription_renewal_cancelled`); **ревизия приёма вебхука [ADR-027](../../adr/ADR-027-adapty-webhook-bearer-token-grant.md) зафиксирована в docs — требует доработки кода** (Bearer вместо HMAC, always-200-on-bad-input, token-grant по тиру); **consumable token-паки + подписки=0 [ADR-038](../../adr/ADR-038-adapty-consumable-token-packs.md) зафиксированы — требуют реализации кода** (`non_subscription_purchase`, `TOKEN_PACK_PRODUCTS`); **прямой StoreKit JWS-путь [ADR-039](../../adr/ADR-039-direct-storekit-jws-purchase-path.md) зафиксирован — требует реализации кода** (`app/billing/storekit.py`, `POST /v1/tokens/purchase` + `/v1/subscription/sync`, таблица `store_transactions`, зависимость `cryptography`). · **Владелец кода:** `app/billing`

Интеграция с Adapty: приём вебхуков, ресинк `getProfile`, маппинг тарифа → квоты, quota-gate, grace-teardown сайтов. Adapty — источник истины по правам ([ADR-004](../../adr/ADR-004-adapty-source-of-truth.md), [ADR-009](../../adr/ADR-009-billing-idempotency-resync-grace.md)).

## Граница
- iOS ведёт покупку через Adapty SDK сам; backend только сверяет entitlements и гейтит генерацию.
- Эндпоинт вебхука авторизуется **Bearer-секретом** `ADAPTY_WEBHOOK_SECRET` (constant-time, **не HMAC**; ревизия [ADR-027](../../adr/ADR-027-adapty-webhook-bearer-token-grant.md)). После авторизации — always-200-on-bad-input. `subscription_started`/`renewed` дополнительно начисляют токены `bonus_generations_balance` по тиру product_id (token-grant дополняет access_level-модель).
- HTTP-роутинг и подключение dependency — модуль `api`; teardown сайтов — модуль `deploy`. `billing` отдаёт `quota_gate`/`entitlements` и инициирует teardown.

## Документы
- [00-overview.md](00-overview.md) — scope, компоненты, граница S5
- [03-architecture.md](03-architecture.md) — webhook handler + маппинг событий, ресинк (beat+lazy), entitlements/quota_gate, usage_counters, grace state-machine
- [02-api-contracts.md](02-api-contracts.md) — `/billing/webhook/adapty`, `/billing/me`, quota-gate `402`

## DoD (прямой StoreKit-путь — [ADR-039](../../adr/ADR-039-direct-storekit-jws-purchase-path.md))
- Верификатор `app/billing/storekit.py`: разбор JWS → x5c cert-chain → доверенный Apple root (`APPSTORE_ROOT_CERT_DIR`) → ES256-подпись leaf (`PyJWT[crypto]`) → валидация payload (`bundleId`/`environment`/`revocationDate`). **fail-closed → `422`** без roots; `VerifiedTransaction` dataclass; payload/JWS не логируются.
- Эндпоинты (клиентский Bearer, тело `{jws}`): `POST /v1/tokens/purchase` (→ `resolve_consumable_tokens`/`grant_tokens`, общий `TOKEN_PACK_PRODUCTS`) + `POST /v1/subscription/sync` (→ `apply_storekit_subscription`: `access_level=pro`/`expires_at`, токены НЕ начисляет). Начисление — на Bearer-`user_id`, не на payload-account.
- **Глобальная идемпотентность** — новая таблица `store_transactions(transaction_id PK)` (блокирует кросс-аккаунтную переигровку); транзакционная миграция (`create_table`, revises `20260617_0001`).
- `grant_tokens` обобщён параметрами `created_by`/`reason`/`idempotency_key` (Adapty-вызовы байт-в-байт прежние; write-path не дублируется).
- Env `APPSTORE_ROOT_CERT_DIR`/`APPSTORE_BUNDLE_ID` (поля `Settings`, api); прямая зависимость `cryptography>=43` в `pyproject.toml`. **Per-instance безопасность:** Xcode-сертификат + пустой bundle-check — только тест/dev; prod = Apple root + реальный bundle id.
- Сосуществование с Adapty без двойного начисления — контракт разграничения каналов (Xcode↔Sandbox/Prod); структурный кросс-дедуп — [Q-BILLING-7](../../99-open-questions.md#q-billing-7).

## DoD (consumable token-паки — [ADR-038](../../adr/ADR-038-adapty-consumable-token-packs.md))
- `non_subscription_purchase` в `KNOWN_EVENT_TYPES` + `CONSUMABLE_EVENT_TYPES`; отдельная ветка `process_webhook` (после user-lookup), **минуя `apply_webhook_event`** — `subscriptions`/`access_level` не трогаются.
- `TOKEN_PACK_PRODUCTS` (env `str` CSV `product_id:amount`) парсится в `dict[str,int]`, fail-fast на невалиде; `resolve_consumable_tokens` → amount или `None` (без fallback).
- Начисление через **общий** grant-примитив (§11.2): `credit_grants(created_by='adapty', reason='adapty:non_subscription_purchase', idempotency_key=event_id)` + relative UPDATE, short-circuit `amount<=0`; `grant_subscription_tokens` рефакторится на этот примитив (без дублирования).
- Неизвестный `vendor_product_id` → `200 ignored:unknown_token_product`, `billing_events processed_at=NULL`, alert; повтор `event_id` → `200 duplicate` (без двойного начисления). Без миграции.
- Подписки → 0 токенов: `SUBSCRIPTION_TOKENS_WEEKLY/YEARLY/GRANT` дефолт → `0`; `SUBSCRIPTION_PRODUCT_*` дефолт → реальные SKU. `non_subscription_purchase_refunded` не обрабатывается (Q-BILLING-6).

## DoD (ревизия приёма вебхука — [ADR-027](../../adr/ADR-027-adapty-webhook-bearer-token-grant.md))
- Bearer-авторизация вебхука (`ADAPTY_WEBHOOK_SECRET`, constant-time) до парсинга тела: `401` на невалид, `500` на пустой секрет; HMAC убран.
- Always-200-on-bad-input: empty_body/invalid_json/not_an_object/unknown-или-ignored event_type/missing_customer_user_id → `200 ignored`; `5xx` только на сбой БД. **Ключ дедупа выводится всегда → тихого дропа `missing_event_id` больше нет** ([ADR-040 §B](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md)).
- Дефенсивный парсинг: ключ дедупа = `event_properties.profile_event_id`→synthetic ([ADR-040 §A](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md)); `subscription_renewal_cancelled` (имя исправлено, status сохраняется, токены не трогаются) в нормативной таблице §2.3; `KNOWN_EVENT_TYPES` = 18 фактических типов Adapty, классификация handled/consumable/ignored — [§2.5](03-architecture.md#25-known_event_types-и-наблюдаемость-отброшенных-событий-adr-040-cd).
- Token-grant: `started`/`renewed` → `bonus_generations_balance += tier_tokens` (WEEKLY/YEARLY/fallback GRANT) в одной транзакции с `billing_events`+`credit_grants(created_by='adapty', idempotency_key=<ключ дедупа>)`; повтор ключа не начисляет повторно. Без миграции.

## DoD (Sprint 3.5)
- Идемпотентный вебхук (UNIQUE `adapty_event_id`), верификация подписи Adapty → `401` на невалид.
- Маппинг `event_type`→`subscriptions` по нормативной таблице ([03-arch §2.3](03-architecture.md#23-маппинг-event_type--subscriptions-нормативная-таблица)).
- `getProfile`-ресинк: периодический beat (`BILLING_RESYNC_INTERVAL_S`) + lazy на гейте; rate-limit к Adapty; fail-open на кэш.
- Сидинг `plan_quotas` (Free+Pro, Alembic data-migration).
- `entitlements` заменяют S3-заглушку free (реальный `access_level` в concurrency-cap).
- quota-gate на `POST /projects` → `402` (`reason`+`required_entitlement`); контракт на `/edits` готов (активен с S5).
- Инкремент `usage_counters` на **успешном старте генерации** (`kind='generation'`), идемпотентно по `job_id`.
- `GET /billing/me` — entitlement + остаток квоты.
- Grace-teardown: `billing.subscription_sweep` (beat) гасит сайты при `grace_until<now`; renew в grace отменяет.

## Открытые пункты
- [Q-BILLING-4](../../99-open-questions.md#q-billing-4) — open: при реальной интеграции Adapty верифицировать webhook signature-header/префикс и точную схему `getProfile` payload v2; обновить контракт-тест httpx-мока под живой sample. Не блокирует код S3.5; целевой — момент реальной интеграции / pre-prod.
- ~~[Q-BILLING-5]~~ — **resolved [ADR-040](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md)** (2026-07-10): сверено с first-party доке Adapty — `subscription_cancelled` не существует, фактическое имя `subscription_renewal_cancelled`; исправлено во всех нормативных точках.
- [Q-BILLING-8](../../99-open-questions.md#q-billing-8) — явная обработка осознанно игнорируемых known-типов (`trial_*`/`subscription_paused`/`subscription_deferred`/`subscription_renewal_reactivated`/`entered_grace_period`) сверх resync-реконсиляции. Follow-up, не блокирует.
- [Q-BILLING-6](../../99-open-questions.md#q-billing-6) — обработка `non_subscription_purchase_refunded` (clawback consumable-токенов): списание при возврате, разрешение отрицательного баланса на израсходованном паке. Вне scope ADR-038; не блокирует начисление паков.
- [Q-BILLING-7](../../99-open-questions.md#q-billing-7) — **структурный кросс-канальный дедуп StoreKit↔Adapty** ([ADR-039](../../adr/ADR-039-direct-storekit-jws-purchase-path.md)): верифицировать равенство «Adapty `transaction_id` == StoreKit `transactionId`» по реальным payload'ам Adapty; при подтверждении — унифицировать идемпотентность обоих каналов на общий `transaction_id`. Пока не подтверждено — контракт разграничения каналов (Xcode → прямой; Sandbox/Prod → Adapty) единственный guard. **Не блокирует** (прямой путь глобально самодостаточно идемпотентен; основной кейс Xcode пересечения не имеет).
- [Q-BILLING-9](../../99-open-questions.md#q-billing-9) — маппинг Adapty `access_level_id`→наш `access_level` при **нескольких** платных тирах ([ADR-041 §B](../../adr/ADR-041-adapty-webhook-field-extraction-real-payload.md)). Follow-up, не блокирует (однотарифная модель `is_active`-достаточна).
- [Q-BILLING-10](../../99-open-questions.md#q-billing-10) — **операционный:** прод `ADAPTY_API_KEY` невалиден → `getProfile`-resync `401`/fail-open (НЕ реконсилит) ⇒ вебхук — единственный источник истины ([ADR-041 §D](../../adr/ADR-041-adapty-webhook-field-extraction-real-payload.md)). Восстановить ключ (ops); не блокирует код-фикс, обязателен для полной prod-корректности.
- [TD-009](../../100-known-tech-debt.md#td-009) — `billing.resync` без батча/LIMIT → Sprint 6 (scale): `.limit(BATCH)` + курсор по `synced_at ASC`.
- **Связь с S4:** sandbox egress-policy не должна блокировать исходящий `getProfile` beat-воркера к Adapty — [05-security → «Граница egress-политики»](../../05-security.md#граница-egress-политики-build-sandbox-vs-application-процессы-требование-к-sprint-4), [Q-DEPLOY-1](../../99-open-questions.md#q-deploy-1).

## Changelog
- 2026-07-10: **прод-фикс field-extraction вебхука Adapty — второй дефект того же класса** ([ADR-041](../../adr/ADR-041-adapty-webhook-field-extraction-real-payload.md), тот же инцидент `nexoraweb.shop`): `apply_webhook_event` читал `access_level`/`expires_at`/`will_renew`/`customer_user_id` из **несуществующих** объектов `payload["profile"]`/`payload["subscription"]` → после фикса ключа ([ADR-040](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md)) события персистятся, но подписочные поля наполнялись пустым/дефолтным (денежный путь починен лишь частично — consumable корректен, подписки нет). Сверено с first-party доке (2026-07-10): в payload **НЕТ** `profile`/`subscription`; фактические места — `expires_at`=`event_properties.subscription_expires_at`/`.expires_at`, `will_renew`=`event_properties.will_renew`→семантика event_type, `access_level`=`pro` константой платного тира (однотарифная модель; `access_level_updated` — по `event_properties.is_active`/`is_in_grace_period`/`is_refund`). Инвариант **«вебхук не понижает права по недостающим данным»** (preserve-on-missing). Зафиксирован прод-факт: `ADAPTY_API_KEY` невалиден → `getProfile`-resync `401`/fail-open (НЕ реконсилит) ⇒ вебхук — единственный источник истины ([Q-BILLING-10](../../99-open-questions.md#q-billing-10)). Метрика диагностики `lovable_billing_webhook_dropped_total{reason,event_type}` зарегистрирована в observability §2.7 (исполнимость требования ADR-040 §D; закрыт висящий `billing_unknown_token_product`). **Rework architect-reviewer (2026-07-10):** (§G) **carve-out `synced_at`** — на `access_level_updated{is_active=false}` (не refund) `apply_webhook_event` НЕ продвигает `synced_at`, иначе бамп на каждом handled-событии бессрочно подавляет resync-backstop, на который опираются §C/§D (согласованная политика `synced_at` для всех мутаторов `subscriptions`: вебхук/admin-grant/StoreKit/resync); (§C) доопределена preserve-семантика на **новой строке** (`expires_at=NULL` = осознанный over-grant, согласован с §12.2; `will_renew`/`access_level` — событийные дефолты §2.3); **deploy-требование** — code-diff ADR-040 и ADR-041 выкатываются **одним changeset** (раздельный выкат ADR-040 понижает платящих до `free`). Без миграции/зависимостей/env; carve-out новой метрики не вводит (покрыт `lovable_adapty_resync_lag_seconds`). Обновлены 02/03/README модуля, ADR-027/ADR-040/ADR-041, observability §2.7/§3, 06-testing, 99-open-questions (Q-BILLING-9/10, Q-ADMIN-1 cross-ref), INDEX. **Код требует реализации** (backend: field-extraction из `event_properties`, preserve-on-missing, `access_level=pro` для подписочных, `synced_at`-carve-out на `access_level_updated{is_active=false}`, метрика-счётчик; qa: contract-тест (c3) на реальном payload без `profile`/`subscription` + preserve-инвариант + `synced_at`-carve-out). Восстановление прод `ADAPTY_API_KEY` — операционная задача ops ([Q-BILLING-10](../../99-open-questions.md#q-billing-10)).
- 2026-07-10: **прод-фикс контракта вебхука Adapty** ([ADR-040](../../adr/ADR-040-adapty-webhook-dedup-key-event-type-reconciliation.md), инцидент `nexoraweb.shop`): 3 реальных вебхука вернули 200 но не обработались/не попали в `billing_events` — ADR-027 §C извлекал ключ как `event_id||id`, которых в payload Adapty нет → тихий дроп денежного события. Сверено с first-party доке (<https://adapty.io/docs/webhook-event-types-and-fields>, 2026-07-10): ключ дедупа = `event_properties.profile_event_id` (UUID)→synthetic (`adapty-syn:{event_type}:{transaction_id}`→`adapty-syn:body:{sha256}`), выводится **всегда** (запрет тихого дропа); `KNOWN_EVENT_TYPES`=18 фактических типов (handled 7 / consumable 1 / consciously-ignored 10); `subscription_cancelled`→`subscription_renewal_cancelled` (Q-BILLING-5 resolved); наблюдаемость отброшенных событий (WARN/INFO по 05-security, без payload). Без миграции/новых зависимостей/env. Обновлены 02/03/README модуля, ADR-027, 03-data-model, 06-testing, 99-open-questions, INDEX. Заведён Q-BILLING-8. **Код требует реализации** (backend: резолвер ключа дедупа, `KNOWN_EVENT_TYPES` + 3 множества, диагностика; qa: contract-тесты на реальном payload). Восстановление 3 дропнутых событий — операционная задача backend/ops.
- 2026-07-08: **прямой StoreKit JWS-путь параллельно Adapty** ([ADR-039](../../adr/ADR-039-direct-storekit-jws-purchase-path.md)): собственный верификатор `app/billing/storekit.py` (x5c cert-chain → Apple root + ES256, fail-closed `422`, payload не логируется) + эндпоинты `POST /v1/tokens/purchase`/`POST /v1/subscription/sync` (клиентский Bearer, начисление на вызывающего `user_id`); переиспользование `resolve_consumable_tokens`/`grant_tokens` (токены) и helper `subscription_state` (pro); **глобальная** идемпотентность новой таблицей `store_transactions(transaction_id PK)`; прямая зависимость `cryptography>=43` (`PyJWT[crypto]` уже в стеке); env `APPSTORE_ROOT_CERT_DIR`/`APPSTORE_BUNDLE_ID` + per-instance безопасность (Xcode-сертификат/пустой bundle-check — только тест/dev). Покрывает Xcode StoreKit Testing (Adapty-вебхук структурно недоступен) + Sandbox/Prod. Сосуществование с Adapty без двойного начисления — контракт разграничения каналов ([Q-BILLING-7](../../99-open-questions.md#q-billing-7) — структурный дедуп). Транзакционная миграция (`store_transactions`, revises `20260617_0001`). Добавлен [§13](03-architecture.md#13-прямой-storekit-путь-adr-039) + [§4 контрактов](02-api-contracts.md#4-прямой-storekit-путь-adr-039); обновлены 02/03/05/06/07 и `store_transactions` в 03-data-model. **Код требует реализации** (backend: верификатор, эндпоинты, `apply_storekit_subscription`, обобщение `grant_tokens`, таблица/миграция, поля `Settings`; devops: `cryptography` в pyproject, env + cert-каталог per-instance).
- 2026-07-08: **consumable token-паки + подписки=0 токенов** ([ADR-038](../../adr/ADR-038-adapty-consumable-token-packs.md)): новый класс события `non_subscription_purchase` (verified first-party Adapty; поле `vendor_product_id`) — отдельная ветка `webhook_handler`, НЕ трогает `subscriptions`/`access_level`, начисляет токены по маппингу `TOKEN_PACK_PRODUCTS` (env, CSV `product_id:amount`) через общий grant-примитив (§11.2, `credit_grants created_by='adapty'`, idempotency_key=event_id); неизвестный product_id → `200 ignored:unknown_token_product` (без угадывания, `processed_at=NULL`, alert). Подписки → **0 бонус-токенов** (`SUBSCRIPTION_TOKENS_*=0`, short-circuit `amount<=0`; только `access_level=pro`). `resolve_consumable_tokens` без fallback. Без миграции/новых полей БД; новый env `TOKEN_PACK_PRODUCTS` + дефолты `SUBSCRIPTION_*` → 0/реальные SKU. Обновлены 02/03 модуля, 06-testing, 07-deployment. **Код требует реализации** (backend: ветка consumable, `resolve_consumable_tokens`, парсер `TOKEN_PACK_PRODUCTS`, рефактор `grant_subscription_tokens`→общий примитив, дефолты config.py). Открыты Q-BILLING-5/6.
- 2026-06-17: **admin-grant pro-подписки** ([ADR-037](../../adr/ADR-037-admin-grant-pro-subscription.md)): новый helper `subscription_state.apply_admin_grant` (переиспускает `_ensure_row`, ставит `access_level=pro`/`status=active`/`store='admin'`, токены НЕ начисляет) для `POST /v1/admin/users/{user_id}/subscription` ([admin §3.5](../admin/03-architecture.md#35-выдача-pro-подписки-adr-037)). Срок параметром (`duration_days`/`expires_at`/бессрочно `NULL`). Без миграции/env/зависимостей. Добавлен [§12](03-architecture.md#12-admin-grant-pro-подписки-adr-037); сосуществование с ресинком/энфорс срока — [Q-ADMIN-1](../../99-open-questions.md#q-admin-1). **Код требует реализации.**
- 2026-06-09: **ревизия приёма вебхука** ([ADR-027](../../adr/ADR-027-adapty-webhook-bearer-token-grant.md)): Bearer заменяет HMAC (constant-time, `401`/`500`), always-200-on-bad-input (5xx только на сбой БД), дефенсивный парсинг, новый `subscription_cancelled`, token-grant по тиру product_id (`SUBSCRIPTION_PRODUCT_*`/`SUBSCRIPTION_TOKENS_*`/fallback `SUBSCRIPTION_TOKENS_GRANT`) с идемпотентностью по `event_id` (одна транзакция с `billing_events`+`credit_grants created_by='adapty'`, без миграции). Обновлены 02/03 модуля, 05-security, 06-testing, 07-deployment. **Код требует доработки.**
- 2026-06-02: создан bootstrap (architect).
- 2026-06-02: зафиксированы решения S3.5 — Free+Pro freemium, лимиты, grace 7д, вебхуки+ресинк, маппинг `customer_user_id=user.id` (Q-BILLING-1/2/3 resolved, [08-product-decisions.md §3.5](../../08-product-decisions.md#sprint-35--billing-adapty)).
- 2026-06-02: развёрнут **исполняемый контракт S3.5** — маппинг событий вебхука, dual-source ресинк (beat+lazy), quota-gate `reason`-коды, `usage_counters` точка инкремента, grace state-machine + sweeper, новые env-ключи, [ADR-009](../../adr/ADR-009-billing-idempotency-resync-grace.md); добавлены `subscriptions.grace_until`/`status=billing_issue` ([03-data-model](../../03-data-model.md#биллинг)).
- 2026-06-02: **Sprint 3.5 реализован** (backend+devops → reviewers → qa 417 passed / coverage 88% → reviewer `production_ready: true`). Статус модуля → «реализован». Живой E2E с реальным Adapty (боевой вебхук+подпись + `getProfile` v2) + Claude+Docker — НЕ прогонялся (проверено через httpx-мок + HMAC тест-секрет + эфемерный Postgres/Redis); открыт [Q-BILLING-4](../../99-open-questions.md#q-billing-4). Заведён [TD-009](../../100-known-tech-debt.md#td-009) (resync без батча → S6). Зафиксировано требование S4: egress-policy не блокирует `getProfile` beat-воркера ([05-security](../../05-security.md#граница-egress-политики-build-sandbox-vs-application-процессы-требование-к-sprint-4)).
