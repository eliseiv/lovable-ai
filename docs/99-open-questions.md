# 99 — Open Questions

Реестр блокеров. Формат `Q-<AREA>-<N>`. Закрытие — решением (обновить затронутые docs) или новым ADR.

---

## Q-DEPLOY-1
**Supply-chain `npm ci` по LLM-сгенерированному `package.json`.** Allowlist registry / vendored baseline / egress-lockdown — какую политику принять, как обрабатывать неизвестные пакеты.
**Решение (Sprint 4, [ADR-010](adr/ADR-010-build-sandbox-rootless-egress.md)):** **egress-lockdown на build (только npm registry) + resource-лимиты.** Build-контейнер в изолированной `BUILD_EGRESS_NETWORK` + egress-proxy, пропускающий **только** `NPM_REGISTRY_ALLOWLIST` (npm-registry); всё остальное (private CIDR, cloud-metadata, прочий интернет) — DROP. `.npmrc` инжектит воркер (не из LLM-дерева). Плюс ресурс-лимиты контейнера (`BUILD_*`). Исполняемый контракт — [ADR-010 §C](adr/ADR-010-build-sandbox-rootless-egress.md), [05-security.md → threat model](05-security.md#threat-model-центр--build-sandbox), [modules/deploy/03-architecture.md §1](modules/deploy/03-architecture.md#1-sandbox-исполнение-недоверенного-кода). Зафиксировано в [08-product-decisions.md §4-4](08-product-decisions.md#sprint-4--sandbox--security). Гасит [TD-001](100-known-tech-debt.md#td-001) совместно с Q-INFRA-1.
**Блокирует:** Sprint 4 (Sandbox & security).
**Уточнение scope egress (S4):** allowlist/lockdown накладывается **только на build-песочницу**, НЕ на доверенные application-процессы (web/worker/beat). При детализации egress-контракта S4 учесть, что исходящий трафик доверенных воркеров к внешним API обязан остаться разрешённым: `getProfile` beat-воркера billing → Adapty ([billing §3.1](modules/billing/03-architecture.md#31-периодический-celery-beat-billingresync)), Anthropic API из пайплайна, Apple JWKS из auth. Нормативная формулировка — [05-security → «Граница egress-политики»](05-security.md#граница-egress-политики-build-sandbox-vs-application-процессы-требование-к-sprint-4) (single normative source).
**Статус:** resolved — **реализовано в Sprint 4** (egress-allowlist через изолированную `BUILD_EGRESS_NETWORK` + egress-proxy `NPM_REGISTRY_ALLOWLIST` + транспорт `BUILD_EGRESS_PROXY_URL`; app-процессы не блокируются). Покрыто unit/integration; гасит [TD-001](100-known-tech-debt.md#td-001). Открытый приёмочный пункт — живой энфорс npm-через-proxy на реальном build-хосте (2 real-stack теста skip), вынесен в приёмочные пункты Sprint 5.

## Q-DEPLOY-2
**TLS `*.apps.domain`.** Wildcard через DNS-01 (какой DNS-провайдер, хранение token) vs per-subdomain HTTP-01 (риск rate-limit Let's Encrypt).
**Решение (resolved, [ADR-017](adr/ADR-017-path-based-site-routing.md)/[ADR-018](adr/ADR-018-prod-deployment-shared-traefik-cicd.md)):** **вопрос снят переходом на path-based routing.** Прод-домен — `corelysite.shop`; сайты раздаются **path-based** `corelysite.shop/s/{site_id}` (НЕ субдомены), API — `corelysite.shop`. Всё на **одном** домене → **один** Let's Encrypt-сертификат, который выпускает **общий edge-Traefik** прод-сервера сам ([ADR-018](adr/ADR-018-prod-deployment-shared-traefik-cicd.md)). **Wildcard `*.apps.domain` не нужен**, DNS-01/DNS-провайдер/token не требуются. Прежняя целевая модель DNS-01 (S4) отменена для prod — историческая, [05-security.md → Целевая модель wildcard TLS](05-security.md#целевая-модель-wildcard-tls-sprint-4--отменена-для-prod-path-based-см-ниже). Нормативно: [ADR-017](adr/ADR-017-path-based-site-routing.md) (path-routing + StripPrefix + Vite base-path), [modules/deploy/03-architecture.md §2A](modules/deploy/03-architecture.md#2a-path-based-routing-s-site_id-prod--site_routing_modepath-adr-017), [07-deployment.md → Prod-модель](07-deployment.md#prod-модель-shared-traefik-corelysiteshop-adr-018), [05-security.md → TLS](05-security.md#tls).
**Блокирует:** (исторически) Sprint 4 wildcard TLS — снят. На prod-релиз — **не блокирует** (TLS выпускает общий Traefik).
**Статус:** **resolved** (path-based снял потребность в wildcard; один сертификат `corelysite.shop` от общего edge-Traefik).

## Q-DEPLOY-3
**Subdomain takeover / GC при удалении проекта.** Очистка **orphaned** контейнеров, Traefik-route, volume, DNS при **удалении проекта**; политика реюза и предотвращение перехвата освободившегося субдомена.
**Не входит сюда:** teardown **текущего** деплоя при фейле health/deploy и при вытеснении новой ревизией — это штатный happy-path-fail, **обязателен в Sprint 1** и зафиксирован в [modules/deploy/03-architecture.md §5](modules/deploy/03-architecture.md#5-lifecycle-сайт-деплоя-state-machine-site_deploymentsstatus). Дельта S1↔S4 — [TD-003](100-known-tech-debt.md#td-003).
**Решение (Sprint 4, [ADR-011](adr/ADR-011-project-delete-gc.md)):** **`DELETE /projects/{pid}` → `202` + soft-delete (`projects.deleted_at`) + асинхронный Celery `project.gc`** — снос всех контейнеров/Traefik-route проекта, volume, S3-артефактов всех ревизий/деплоев, БД-каскад; in-flight джобы → `FAILED(project_deleted)`; идемпотентно. Субдомены opaque (`[a-z0-9]{16}`) и **не реюзаются** — защита от takeover. Исполняемый контракт — [ADR-011](adr/ADR-011-project-delete-gc.md), [modules/deploy/03-architecture.md §6](modules/deploy/03-architecture.md#6-gc-при-удалении-проекта-sprint-4--delete-projectsid-adr-011-закрывает-td-003q-deploy-3), [modules/api/02-api-contracts.md → DELETE](modules/api/02-api-contracts.md#delete-projectspid-sprint-4). Зафиксировано в [08-product-decisions.md §4-5](08-product-decisions.md#sprint-4--sandbox--security). Гасит [TD-003](100-known-tech-debt.md#td-003).
**Блокирует:** Sprint 4; частично Sprint 5 (teardown ревизий).
**Статус:** resolved — **реализовано в Sprint 4** (`DELETE /projects/{pid}`→`202`+soft-delete + Celery `project.gc`: полный teardown контейнеров/route/volume/S3/БД-каскад, in-flight→`FAILED(project_deleted)`, идемпотентно, субдомены не реюзаются). Покрыто тестами; гасит [TD-003](100-known-tech-debt.md#td-003). Остаточный наблюдаемый эффект eventual-окна GC — [TD-010](100-known-tech-debt.md#td-010) (метрика/алерт → S6).

## Q-DEPLOY-4
**CI/CD для второго (клон) инстанса.** Текущий GitHub Actions workflow деплоит **только** в `/opt/corelysite` ([07-deployment.md → CI/CD](07-deployment.md#cicd-контракт-github-actions-adr-018)). Нужно ли автоматизировать деплой второго инстанса (`nexoraweb` и далее) через CI — параметризовать workflow matrix/список по `{каталог, project-name, env-файл}` — или достаточно ручного деплоя.
**Решение (resolved, 2026-06-04, [07-deployment.md → CI/CD-контракт](07-deployment.md#cicd-контракт-github-actions-adr-018) + [ADR-018 → Мульти-инстанс](adr/ADR-018-prod-deployment-shared-traefik-cicd.md#примечание-мульти-инстанс--клон-сервиса-обратносовместимая-параметризация)):** **CI авто-деплоит ВСЕ прод-инстансы.** Единый `deploy`-job на push в `main` (после зелёных гейтов `needs:[lint,type-check,test]`) деплоит **последовательно** каждый инстанс из нормативного списка `{dir, project}` — `corelysite` (`/opt/corelysite`, `-p corelysite`) **и** `nexoraweb` (`/opt/nexoraweb`, `-p nexoraweb`): на инстанс `cd /opt/<dir> && git pull --ff-only && docker compose -p <project> -f infra/docker-compose.prod.yml --env-file .env up -d --build`. **Изоляция отказа:** fail-fast по каждому инстансу, провал одного **не** откатывает соседа (инстансы независимы; общие — лишь сеть `web`, edge-Traefik, rootless `docker.sock`). **Миграции** — per-инстанс на своей БД (свой `migrate`-гейт, тома изолированы project-name). **Расширяемость** — список инстансов в workflow: добавление третьего = +1 строка `{dir, project}` (+ первичная настройка каталога/`.env`/host-каталогов). Ручной [деплой клона](07-deployment.md#процедура-деплоя-клона-нормативная) остаётся для **первичной** настройки инстанса и аварийного деплоя, но регулярный push-деплой — авто для всех инстансов. Сохранены: SHA-pinned third-party actions, `--env-file .env`, секреты через GitHub Secrets.
**Реализация (devops):** правка `.github/workflows/ci.yml` `deploy`-job — добавить деплой `nexoraweb` к существующему `corelysite` (список/цикл/matrix инстансов), точные per-инстанс команды по контракту. Гейты/триггер (`if push main`)/SHA-pin/`--env-file` не менять.
**Блокирует:** **НЕ блокирует** запуск клона (ручной деплой остаётся самодостаточным для первичной настройки).
**Статус:** **resolved** — авто-деплой обоих инстансов зафиксирован в контракте; реализация — devops по [CI/CD-контракту](07-deployment.md#cicd-контракт-github-actions-adr-018).

## Q-PIPELINE-1
**Контракт output Agent 3.** Строгая валидируемая схема дерева файлов (пути, бинарность, лимиты размера) до сборки — чтобы фейлить рано, до песочницы.
**Решение:** полная валидируемая схема зафиксирована в [modules/pipeline/03-architecture.md → «Контракт output Agent 3»](modules/pipeline/03-architecture.md): безопасные относительные пути (без traversal/абсолютных/симлинков), бинарность через `encoding` utf8/base64, лимиты `MAX_FILES`/`MAX_FILE_BYTES`/`MAX_TREE_BYTES`, обязательные `package.json`+entry, allowlist расширений (запрет `.npmrc`/dotfiles). Достаточно для DoD Sprint 1. Контроль содержимого зависимостей (`npm ci` supply-chain) — отдельный слой, остаётся за [Q-DEPLOY-1](#q-deploy-1) (Sprint 4).
**Статус:** closed-for-S1 (остаток supply-chain → Sprint 4 / Q-DEPLOY-1).

## Q-PIPELINE-2
**Идемпотентность `/answers`.** Поведение при двойном сабмите / уже продвинувшейся джобе (повторный `POST /answers` на `SPECCING`/`LIVE`).
**Решение:** детерминированная матрица зафиксирована в [modules/api/02-api-contracts.md → «POST /jobs/{jid}/answers»](modules/api/02-api-contracts.md): повтор тех же ответов → идемпотентный `200`/`202` с тем же `job_id`; джоба уже в `SPECCING`/`BUILDING`/`DEPLOYING`/`LIVE`/`FIXING`/`FAILED` с другими ответами → `409` (RFC-7807); частичные/конфликтующие/чужие `question_id` → `422`.
**Статус:** closed-for-S1.

## Q-COST-1
**Дефолты бюджетов и kill по бюджету.** Значения `job_budget_usd`/`monthly_budget_usd` по умолчанию; поведение при убийстве джобы по бюджету (cleanup песочницы/контейнера, сообщение пользователю, состояние `FAILED(budget_exhausted)`).
**Решение (Sprint 2):** дефолты resilience-гардов зафиксированы в env-контракте и [modules/pipeline/03-architecture.md → §C](modules/pipeline/03-architecture.md#c-четыре-гарда-от-бесконечного-цикла-и-runaway-затрат): `JOB_BUDGET_USD`=$5.0000, `USER_MONTHLY_BUDGET_USD`=$50.0000, `MAX_FIX_ATTEMPTS`=3, `JOB_WALL_CLOCK_BUDGET_S`=3600 s, `CLARIFICATION_TTL_S`=7 дней. **Kill-by-budget:** проверка `spend_usd>=budget_usd` перед каждым LLM-вызовом → прерывание витка без нового Claude-вызова; deploy-ресурсы уже сняты teardown'ом (фейл произошёл до входа в FIXING); пользователю — последний `failure_log` + `failure_reason=budget_exhausted`. Точная калибровка дефолтов под реальную стоимость — Sprint 6 (cost).
**Блокирует:** Sprint 2 (resilience). Остаток (калибровка/дашборды $) → Sprint 6.
**Статус:** closed-for-S2 (калибровка дефолтов → Sprint 6).

## Q-INFRA-1
**Топология build-хостов.** DinD vs shared Docker socket vs dedicated VM на джобу — сила изоляции песочницы vs модель масштабирования build-фермы.
**Текущая модель (S1):** shared `docker.sock` без gVisor — принятый компромисс, заведён как [TD-001](100-known-tech-debt.md#td-001). Спринт-разметка изоляции — [07-deployment.md → «Модель изоляции сборки по спринтам»](07-deployment.md#модель-изоляции-сборки-по-спринтам). Этот Q закрывает выбор **целевой** топологии в Sprint 4.
**Решение (Sprint 4, [ADR-010](adr/ADR-010-build-sandbox-rootless-egress.md)):** **rootless Docker + egress-allowlist** для build-песочницы (вместо gVisor/`runsc` или shared `docker.sock`). Build-воркеры → rootless Docker-демон (`BUILD_SANDBOX_RUNTIME=rootless`) на отдельных build-хостах; `runsc`/gVisor зарезервирован опционально. Зафиксировано в [08-product-decisions.md §4-3](08-product-decisions.md#sprint-4--sandbox--security), исполняемый контракт — [ADR-010 §A](adr/ADR-010-build-sandbox-rootless-egress.md). Совместно с [Q-DEPLOY-1](#q-deploy-1) гасит [TD-001](100-known-tech-debt.md#td-001).
**Блокирует:** Sprint 4 (закрытие → погашение TD-001); влияет на Sprint 6 (scale).
**Статус:** resolved — **реализовано в Sprint 4** (build-воркер на rootless Docker-сокете `BUILD_SANDBOX_RUNTIME=rootless` вместо привилегированного `docker.sock`; build-контейнер изолирован cap-drop/seccomp/`--read-only`/non-root/лимиты `BUILD_*`). Покрыто unit/integration; совместно с [Q-DEPLOY-1](#q-deploy-1) гасит [TD-001](100-known-tech-debt.md#td-001). Открытый приёмочный пункт — живой rootless-энфорс на реальном build-хосте (2 real-stack теста skip), вынесен в приёмочные пункты Sprint 5.

## Q-CLIENT-1
**Нотификации при backgrounded iOS.** APNs push vs silent push; путь доставки статуса `LIVE`/`FAILED`, когда приложение в фоне.
**Решение (Sprint 5):** **APNs push** (доставка статуса `LIVE`/`FAILED` в фоне) + SSE/polling в foreground. Зависимость: APNs-ключ/сертификаты от пользователя (**внешняя**, отмечена в [08-product-decisions.md §5-1](08-product-decisions.md#sprint-5--realtime--edits)).
**Блокирует:** Sprint 5 (Realtime & edits).
**Статус:** **resolved (APNs реализован в Sprint 5)** — `notify.apns_push` из `job_events`, `device_tokens`, `POST/DELETE /v1/devices` реализованы и покрыты тестами ([ADR-013](adr/ADR-013-apns-push-from-job-events.md), [modules/notify](modules/notify/README.md), README → Статус Sprint 5). Без APNs `.p8`/`APNS_*` (внешняя зависимость пользователя, Apple Developer) push — no-op, пайплайн цел (проверено тестом). Остаточный live-приёмочный пункт (реальный push боевым `.p8`) — в составе E2E Sprint 5, не реоткрывает Q.

## Q-BILLING-1
**Модель тарифов и квот.** Какие access levels в Adapty; лимиты на тариф (генераций/мес, конкурентных джоб, проектов); поведение при downgrade / grace-period / refund — отзывать ли уже задеплоенные сайты.
**Решение (Sprint 3.5):** тарифы **Free + Pro** (freemium); access_level имена `free`/`pro` (premium = `pro`). Лимиты — Free: 3 ген/мес, 1 проект, 1 конкурентная; Pro: 100 ген/мес, безлимит проектов, 3 конкурентных (нормативная таблица сидинга `plan_quotas` — [08-product-decisions.md → Sprint 3.5](08-product-decisions.md#sprint-35--billing-adapty)). **Сайты при отмене/refund:** grace 7 дней → teardown; renew в grace → сайт остаётся. Реальные Adapty product IDs привяжутся в дашборде Adapty позже (**зависимость**).
**Блокирует:** Sprint 3.5 (Billing) — сидинг `plan_quotas`.
**Статус:** resolved.

## Q-BILLING-2
**Сверка прав на гейте.** Доверять только вебхукам или всегда дополнительно `getProfile` на гейте (latency vs консистентность при пропущенных вебхуках); частота фоновой ресинхронизации (`getProfile`-ресинк через beat). Уточняет [ADR-004](adr/ADR-004-adapty-source-of-truth.md).
**Решение (Sprint 3.5):** **вебхуки Adapty — источник истины** (низкая latency на гейте читаем кэш `subscriptions`), **+ периодический `getProfile`-ресинк через beat** как fallback на пропущенные вебхуки. Дополнительный `getProfile` на каждом гейте по умолчанию НЕ делаем (только фоновый ресинк). Зафиксировано в [08-product-decisions.md §3.5-5](08-product-decisions.md#sprint-35--billing-adapty), согласовано с [ADR-004](adr/ADR-004-adapty-source-of-truth.md).
**Блокирует:** Sprint 3.5.
**Статус:** resolved.

## Q-BILLING-3
**Маппинг пользователя ↔ Adapty.** Связка нашего `user.id` ↔ Adapty `customer_user_id`; момент создания (первый запуск iOS vs первая покупка); обработка анонимных профилей.
**Решение (Sprint 3 / 3.5):** Adapty `customer_user_id` = наш `user.id`. Создаётся **при первом входе iOS** (Sign in with Apple, [ADR-007](adr/ADR-007-sign-in-with-apple.md)) — не при первой покупке, поэтому анонимных профилей не возникает. Вебхук с неизвестным `customer_user_id` (теоретически — рассинхрон) записывается в `billing_events` с `user_id = NULL`, `processed_at = NULL` для последующей обработки/алерта. Зафиксировано в [08-product-decisions.md §3.5-4](08-product-decisions.md#sprint-35--billing-adapty).
**Блокирует:** Sprint 3 (привязка `adapty_customer_user_id`) и Sprint 3.5.
**Статус:** resolved.

## Q-BILLING-4
**Точная схема верификации вебхука и `getProfile` payload v2 при реальной интеграции Adapty.** Код S3.5 реализован и протестирован против **httpx-мока Adapty + HMAC тест-секрета** ([06-testing-strategy](06-testing-strategy.md)), но боевые детали Adapty не верифицированы на живом сервисе:
1. **Webhook signature.** Имя header'а и формат подписи (предположительно `adapty-signature`, HMAC-SHA256 hex **без** префикса схемы — в отличие, например, от Stripe `t=…,v1=…`) — нужно подтвердить против реальной доставки Adapty. Текущая реализация верификации (`ADAPTY_WEBHOOK_SECRET`, HMAC/`hashlib` stdlib, [05-security §Adapty webhook](05-security.md#adapty-webhook-не-bearer)) рассчитана на hex-HMAC без префикса.
2. **`getProfile` payload v2.** Точная структура ответа Adapty Server-side API v2 (`access_levels` map vs список, поля `subscriptions[]`, имена `is_active`/`will_renew`/`expires_at`) — реализация маппинга ([billing §2.3/§3](modules/billing/03-architecture.md#23-маппинг-event_type--subscriptions-нормативная-таблица)) основана на предполагаемой схеме v2; контракт-тест httpx-мока обновить под **живой sample** при первой реальной интеграции.
**Решение:** не зафиксировано — требует доступа к боевому Adapty (sandbox/dashboard). При реальной интеграции: снять живой webhook-sample + `getProfile`-sample, подтвердить/скорректировать header-имя/префикс подписи и схему payload, обновить контракт-тест httpx-мока под живой sample. Расхождение с предположением → правка `app/billing/webhook_handler` (парсинг подписи) и `adapty_client`/маппинга — **код-правка, не пересмотр ADR-009** (механика dual-source/идемпотентности/grace не меняется).
**Блокирует:** **не блокирует код Sprint 3.5** (реализован и зелёный на моках). Целевой — **момент реальной интеграции Adapty / перед prod-запуском billing**. `blocks_sprint: none` (ответ нужен к первому боевому подключению Adapty, не к спринту разработки).
**Статус:** open (целевой — реальная интеграция / pre-prod). **На завершение проекта (после Sprint 6):** остаётся единственным open-Q; **не блокирует** код ни одного спринта — числится в остаточных приёмочных пунктах prod-релиза ([README → Остаточные приёмочные пункты, п.2](README.md#остаточные-приёмочные-пункты-для-prod-релиза-живой-e2e-на-боевом-стеке)).

## Q-AUTH-1
**Слияние двух существующих аккаунтов (account-merge) при кросс-платформенной аутентификации.** [ADR-024](adr/ADR-024-user-id-secret-authentication.md) вводит вход по `user_id`+секрет рядом с Sign in with Apple. Сценарий «перенос/восстановление» для **своего** аккаунта закрыт `POST /v1/auth/secret` (Apple-юзер ставит секрет на свой аккаунт → входит с не-Apple клиента). **Открыт** случай, когда у одного человека уже есть **два разных** аккаунта — Apple-созданный и отдельно `register`-созданный — и нужно слить их в один (перенос проектов/подписок/usage/бонус-кредитов под единый `user_id`).
**Решение:** не зафиксировано — требует продуктового решения по семантике merge (какой `user_id` выживает, как мигрируют `projects`/`subscriptions`/`usage_counters`/`credit_grants`/`api_tokens`, что с Adapty `customer_user_id`). Для **MVP** ([ADR-024](adr/ADR-024-user-id-secret-authentication.md)) `register` создаёт **новый** аккаунт; «привязка» = постановка секрета на *свой текущий* аккаунт через `/auth/secret`, без merge двух существующих.
**Блокирует:** **не блокирует** фичу ADR-024 (register/login/secret реализуемы без merge). `blocks_sprint: none` — отдельное продуктовое решение, не входит в scope текущей фичи.
**Статус:** open (вне MVP клиентской аутентификации по секрету; ответ нужен только если появится продуктовое требование объединять уже существующие раздельные аккаунты).

## Q-LOCALE-1
**Явный locale-override языка сайта от iOS-клиента.** [ADR-025](adr/ADR-025-content-language-autodetect-spec-marker.md) (ревизия [ADR-028](adr/ADR-028-deterministic-source-prompt-language-detection.md)) фиксирует язык контента сайта = **язык ИСХОДНОГО промпта пользователя** (детерминированный серверный детект → `generation_jobs.content_language` → серверная директива агентам → маркер `**Content language:**` в `spec_markdown`). **Открыт** случай, когда пользователь хочет сайт на языке, **отличном** от языка своего ввода (например, пишет промпт по-русски, но хочет англоязычный сайт): нужен ли явный параметр `locale`/`language` от клиента (`POST /projects`), переопределяющий детект, и как он прокидывается агентам.
**Решение (resolved, 2026-06-17, [ADR-036](adr/ADR-036-explicit-client-locale-override.md)):** **явный locale-override вводится.** Опциональное Form-поле `locale` в multipart `POST /v1/projects` (BCP-47 → нормализуется новой `normalize_locale` в `ru`/`en`/`None`; неподдерживаемое/пустое → `None` = **fallback на авто-детект**, НЕ `422`) → хранится в новом nullable-поле `projects.requested_locale` (`NULL` = авто-детект). В `_interview`: `requested_locale` задан ⇒ `language = language_from_bcp47(requested_locale)` (приоритет, без `detect_language`); иначе `detect_language(project.prompt)` (байт-в-байт как прежде). **Ревизует [ADR-028 §4](adr/ADR-028-deterministic-source-prompt-language-detection.md)** (приоритет: явный locale > script-детект; механизм детекта/`content_language`/директива/маркер сохранены). **Scope = только генерация** — правки (`POST /edits`) наследуют язык сайта через маркер `**Content language:**`, контракт `/edits` не меняется. Обратная совместимость (без `locale` → `NULL` → детект байт-в-байт), идемпотентность `content_language` (crash-resume не передетектит). Нормативно — [ADR-036](adr/ADR-036-explicit-client-locale-override.md), [pipeline §Язык/локализация](modules/pipeline/03-architecture.md#языклокализация-контента-сайта--детерминированный-детект-adr-028-ревизует-adr-025), [api/02-api-contracts → POST /projects](modules/api/02-api-contracts.md#post-projects), [03-data-model → projects](03-data-model.md#projects).
**Прод-триггер:** iOS-приложение подмешивало в `project.prompt` длинный английский boilerplate (до ~1800 символов) → латиница перевешивала короткий русский запрос → `content_language=en` (все 4 последние реальные прод-генерации = `en` при русском запросе).
**Блокирует:** не блокирует (обратносовместимая опциональная фича). Без env-ключей (devops не требуется), без новых зависимостей.
**Статус:** **resolved (2026-06-17, [ADR-036](adr/ADR-036-explicit-client-locale-override.md))**.

## Q-LLM-1
**Финализация и верификация GPT-5.5-class моделей OpenAI: model-IDs per-agent, pricing, поддерживаемые reasoning-уровни.** [ADR-032](adr/ADR-032-llm-provider-abstraction-openai.md) вводит OpenAI как альтернативный LLM-провайдер (`LLM_PROVIDER=openai`, мультиинстанс-клон). Контракт абстракции (протокол `run_agent → AgentCall`, фабрика, env-ключи, маппинг thinking/effort→`reasoning.effort`+`max_output_tokens`, usage-маппинг, retry-классификация, авто-кэширование) спроектирован **независимо от точных чисел**. Финализированы три внешне-зависимых от каталога OpenAI значения:
1. **Model-IDs per-agent (нормативно).** Agent 2 → `gpt-5.5` (топовая, spec-writer — аналог Opus); Agent 1/3/4 → `gpt-5.4-mini` (дешёвый tier — аналог Sonnet). Каталог содержит семейства **gpt-5.5** и **gpt-5.4**; устаревшие `gpt-5.1`/`gpt-5-mini` в каталоге **отсутствуют** и заменены. Нормативный источник — [ADR-032 §2](adr/ADR-032-llm-provider-abstraction-openai.md).
2. **Pricing (per-1M USD, Standard-tier, верифицировано).** Нормативная таблица (единственный источник чисел) — [observability §2.2A](modules/observability/03-architecture.md#22a-openai-pricing-провайдер-openai-adr-032): `gpt-5.5` $5.00/$0.50/$30.00; `gpt-5.5-pro` $30.00/—/$180.00; `gpt-5.4` $2.50/$0.25/$15.00; `gpt-5.4-mini` $0.75/$0.075/$4.50; `gpt-5.4-nano` $0.20/$0.02/$1.25; `gpt-5.4-pro` $30.00/—/$180.00. `cache_write`=`0` (caching автоматический, без write-ставки).
3. **Reasoning-уровни (верифицировано).** Реальный набор GPT-5.5-class — `none`/`low`/`medium` (default)/`high`/`xhigh`; **уровня `minimal` НЕ существует**. Agent 3/4 → `none` (disabled-эквивалент, весь cap под вывод); Agent 1/2 → `OPENAI_AGENT_EFFORT` дефолт `high` (из {`medium`,`high`,`xhigh`}). Модельный потолок `gpt-5.5` `max_output_tokens=128000` ≥ per-agent cap (макс. 56000, [ADR-023](adr/ADR-023-agent3-token-budget-thinking-room.md)).
**Решение (resolved, 2026-06-15):** верифицировано по каталогу OpenAI (источники: developers.openai.com/api/docs/pricing и .../models/gpt-5.5; дата верификации 2026-06-15). Дефолты model-IDs/pricing/effort зафиксированы как нормативные; agent→model остаётся operator-tunable через env `AGENTn_MODEL` без релиза (как Anthropic-tiering, [ADR-023](adr/ADR-023-agent3-token-budget-thinking-room.md) R1). Нормативные источники: [ADR-032 §2](adr/ADR-032-llm-provider-abstraction-openai.md) (model+effort), [observability §2.2A](modules/observability/03-architecture.md#22a-openai-pricing-провайдер-openai-adr-032) (pricing).
**Блокирует:** `blocks_sprint`: **имплементация OpenAI-провайдера** — теперь **разблокировано** (числа верифицированы, backend может зашивать pricing-таблицу и дефолтные model-IDs). **НЕ блокировало** Anthropic-путь и проектирование слоя абстракции.
**Статус:** **resolved (2026-06-15)** — model-IDs, pricing и reasoning-уровни верифицированы по каталогу OpenAI и зафиксированы как нормативные.

## Q-LLM-2
**Семантика read/idle-таймаута на SSE-стриме LLM-SDK — живая staging-верификация.** [ADR-035](adr/ADR-035-llm-stream-read-timeout.md) задаёт read/idle-таймаут на LLM-клиентах через `httpx.Timeout(read=LLM_READ_TIMEOUT_S=180, connect=…)`, рассчитывая, что read-таймаут httpx **сбрасывается на каждом SSE-чанке** (таймаут отдельной socket-read-операции, не всего ответа) ⇒ повисший («молчащий») stream падает `APITimeoutError` за ~180 c, а легитимный длинный thinking-стрим (чанки капают) **не рвётся**. Это документированное поведение httpx, но **точная** манера проброса per-request read-таймаута именно на SSE-итератор (`messages.stream`/`responses.stream`) каждого SDK **не верифицирована на живом боевом стеке** этого проекта.
**Решение:** read-таймаут принят как корректный механизм (нормативно — [ADR-035 §Decision (1)](adr/ADR-035-llm-stream-read-timeout.md)). **Безопасен по умолчанию:** худший правдоподобный отказ — таймаут не сработает, поведение останется как сейчас (повис до reconciler-`stuck_timeout`); read-таймаут **не может** ложно оборвать активно идущий стрим (чанки сбрасывают таймер). Поэтому ADR им **не блокируется**. **Staging-проверка (по аналогии с [Q-IMG-1](#q-img-1)):** (а) искусственно повисший stream даёт `APITimeoutError` за ~`LLM_READ_TIMEOUT_S`, джоба уходит в транзиентный ретрай (не `stuck_timeout`); (б) реальный длинный thinking-вызов Agent 2 (Opus+adaptive) завершается успешно, не оборванный read-таймаутом. Если (б) показывает ложные обрывы — поднять `LLM_READ_TIMEOUT_S` env без релиза.
**Блокирует:** **не блокирует** реализацию ADR-035 (код пишется с дефолтным read=180; механизм безопасен по умолчанию). `blocks_sprint: none` — pre-rollout приёмочный пункт (как live-E2E прочих фич), не блокер кода.
**Статус:** open (целевой — staging-прогон: повисший stream → `APITimeoutError`+ретрай; длинный легитимный стрим Agent 2 не рвётся).

## Q-IMG-1
**Совместимость vision (image content-блоков) с extended thinking на Anthropic — живая интеграционная проверка ДО раскатки.** [ADR-034](adr/ADR-034-user-image-attachments-vision-site-assets.md) подаёт приложенные изображения как image content-блоки во вход Agent 1/2 (`thinking=adaptive`). [ADR-020 §Ограничение API](adr/ADR-020-agent-structured-output-tool-use-tolerant-parse-retry.md) зафиксировал HTTP 400 при `thinking + форсированный tool_choice` (и assistant-prefill) — **image-блоки иной механизм, документированно совместимы с thinking**, но это **не проверено на живом Anthropic** в данном проекте.
**Решение:** image-блоки приняты как совместимые с `thinking=adaptive` (нормативно — [ADR-034 §Главный риск](adr/ADR-034-user-image-attachments-vision-site-assets.md)). **Развязка при опровержении:** если staging-вызов Agent 1/2 с `thinking=adaptive` + image вернёт HTTP 400 — реализуется **per-agent `thinking=disabled` ТОЛЬКО для vision-вызовов** (механизм `settings.agent_thinking(agent)` уже есть, [ADR-023](adr/ADR-023-agent3-token-budget-thinking-room.md); override при непустом `images`). Текстовые вызовы без фото — прежний `adaptive`. OpenAI-путь (`reasoning` + `input_image`) риска не несёт.
**Блокирует:** **раскатку vision-фичи на Anthropic-инстансы** (пока staging-проверка не пройдена — vision не включается на боевых Anthropic-инстансах). `blocks_sprint: none` — это **pre-rollout приёмочный пункт** (как live-E2E прочих фич, [06-testing-strategy → Image attachments E2E](06-testing-strategy.md#image-attachments-e2e-фото-на-сайте-adr-034)), а **не** блокер кода реализации (код пишется с дефолтным `adaptive`; развязка — узкий условный override, готов к включению). Не блокирует OpenAI-инстансы.
**Статус:** open (целевой — staging-прогон перед раскаткой vision; разрешается живой проверкой 200-ответа Anthropic на `thinking+image` либо включением развязки).

## Q-IMG-2
**Анти-полиглот перекодирование изображений (Pillow re-encode) — MVP vs follow-up.** [ADR-034 §D2](adr/ADR-034-user-image-attachments-vision-site-assets.md) для MVP валидирует приложенные изображения **sniff'ом magic bytes + лимитами** (тип/размер/число/сторона). Polyglot-файл (валидная image-сигнатура + посторонняя нагрузка-хвост) sniff пропускает; на статик-хостинге он отдаётся nginx как статика с image-MIME и исполняемым не становится, но «чистым» изображением не гарантирован.
**Решение (MVP):** **re-encode НЕ вводится в MVP** — sniff + лимиты + статик-отдача дают достаточную первичную защиту; `MAX_IMAGE_DIMENSION_PX` в MVP проверяется декодированием размеров без обязательного полного re-encode. **Follow-up:** опциональный Pillow-ресайз/перекодирование (нормализация в чистый PNG/JPEG/WebP, срез метаданных/хвостов) — отдельным решением при появлении требования по безопасности/нормализации; потребует прямой зависимости Pillow ([02-tech-stack](02-tech-stack.md)) — см. Q-IMG-3.
**Блокирует:** **не блокирует** фичу ADR-034 (приём/инжект/vision реализуемы на sniff+лимитах). `blocks_sprint: none`.
**Статус:** open (follow-up; ответ нужен только при требовании полной нормализации/анти-полиглота сверх sniff+лимитов).

## Q-IMG-3
**Зависимость для валидации изображений (sniff/размеры/опц. re-encode).** [ADR-034](adr/ADR-034-user-image-attachments-vision-site-assets.md) требует sniff magic bytes (PNG/JPEG/WebP/GIF) и проверку `MAX_IMAGE_DIMENSION_PX` (размеры в пикселях).
**Решение:** **sniff magic bytes реализуется без новой библиотеки** (сравнение байт-сигнатур — чистый Python, как `extract_json` [ADR-020](adr/ADR-020-agent-structured-output-tool-use-tolerant-parse-retry.md)). **Декодирование размеров** (width/height) для `MAX_IMAGE_DIMENSION_PX`: либо узкий ручной парсинг заголовков (PNG IHDR / JPEG SOF / GIF LSD / WebP VP8X — чистый Python), либо **Pillow** при выборе re-encode (Q-IMG-2). **Если вводится любая прямая зависимость (Pillow) — она ОБЯЗАНА быть объявлена в [02-tech-stack.md](02-tech-stack.md)** в той же итерации (правило проекта: внешняя библиотека → `02-tech-stack`), не транзитивно. MVP-дефолт — без новой зависимости (ручной парсинг заголовков размеров).
**Блокирует:** **не блокирует** фичу (MVP реализуем чистым Python). `blocks_sprint: none` — фиксирует требование декларировать Pillow в стеке, **если** backend выберет его для размеров/re-encode.
**Статус:** open (решается при реализации: чистый Python без зависимости — дефолт; Pillow — только с декларацией в `02-tech-stack`).

## Q-ADMIN-1
**Сосуществование admin-grant pro-подписки с Adapty-ресинком и энфорс срока.** [ADR-037](adr/ADR-037-admin-grant-pro-subscription.md) выдаёт pro записью в `subscriptions` (`access_level=pro`, `status=active`, `store='admin'`) через `apply_admin_grant`. Два смежных вопроса: (1) **ресинк может сбросить grant** — `subscriptions` это кэш Adapty (source-of-truth — Adapty, [ADR-004](adr/ADR-004-adapty-source-of-truth.md)/[ADR-009](adr/ADR-009-billing-idempotency-resync-grace.md)); периодический `getProfile`-ресинк ([billing §3.1](modules/billing/03-architecture.md#31-периодический-celery-beat-billingresync)) при неактивном реальном профиле переведёт строку в `expired` (admin-grant потерян), а вебхук перезапишет по state-machine §2.3; (2) **срок не энфорсится** — ни quota-gate, ни `subscription_sweep` не консультируют `expires_at` для admin-grant ([ADR-037 §C](adr/ADR-037-admin-grant-pro-subscription.md)), поэтому заданный `duration_days`/`expires_at` сейчас информативен (pro по сроку автоматически не снимается).
**Текущее (осознанное) поведение, нормативно [ADR-037 §Consequences](adr/ADR-037-admin-grant-pro-subscription.md):** admin-grant предназначен для юзеров **без активной реальной Adapty-подписки** (тестовые/комплиментарные аккаунты); для юзера с живой Adapty-подпиской grant — временный override до следующего ресинка/вебхука. Снятие pro по сроку — повторный вызов эндпоинта / ручная правка. `synced_at=now()` откладывает периодический ресинк на один `BILLING_RESYNC_INTERVAL_S`, но не защищает от вебхука/протухания.
**Возможное решение (follow-up, отдельным ADR при появлении требования):** «admin-pinned» семантика — флаг (например `store='admin'` уже маркирует), который (а) ресинк/вебхук **не** перетирают, и (б) отдельная sweep-ветка снимает pro по `expires_at < now()` (`access_level=pro AND store='admin' AND expires_at IS NOT NULL AND expires_at < now() → access_level=free`). Потребует расширения `resync`/`subscription_sweeper` и, возможно, миграции (явный pin-флаг вместо `store`-конвенции).
**Блокирует:** **не блокирует** ADR-037 (базовое требование — выдать pro гибким сроком, включая бессрочно — выполнено; сосуществование/энфорс срока — осознанные следствия). `blocks_sprint: none` — follow-up, ответ нужен при требовании автоматического энфорса срока или защиты grant от ресинка.
**Статус:** open (follow-up; решается при требовании admin-pinned-семантики / авто-снятия pro по сроку).
