# 100 — Known Tech Debt

Реестр осознанного технического долга. Формат `TD-NNN`. Любой `TODO/FIXME/TBD` в коде/docs без cross-ref на `TD-NNN` или `Q-NNN-N` — блокер.

> **Итоговый статус (после Sprint 6 — завершение проекта): все TD-001..012 закрыты.** TD-001/003 (S4), TD-002 (S1), TD-004 (S3), TD-007/008/009/010/011/012 (S6) — CLOSED. TD-005/006 — CLOSED-for-S6 (метрики-драйверы калибровки и Redis budget-кэш реализованы+покрыты тестами; остаток — тонкая калибровка по дашбордам пост-релиз, не пробел). Подробности — [README → Итоговый статус проекта](README.md#итоговый-статус-проекта-все-6-спринтов--завершение).

| ID | Описание | Введён | План погашения |
|---|---|---|---|
| <a id="td-002"></a>TD-002 ✅ **CLOSED (Sprint 1)** | **[ПОГАШЕН]** ~~Compose/`.env.example` рассогласованы с контрактом env.~~ В Sprint 1 devops привёл `infra/docker-compose.dev.yml`, `minio-setup` и `.env.example` к каноническому env-контракту ([07-deployment.md → «Контракт переменных окружения»](07-deployment.md#контракт-переменных-окружения-environment-reference)): канонические имена `S3_ACCESS_KEY`/`S3_SECRET_KEY`, единый `S3_BUCKET` + key-префиксы, `TRAEFIK_NETWORK`/`NGINX_IMAGE`, `ENVIRONMENT`, плюс `S3_USE_SSL`/`SITES_HOST_ROOT`/`BUILDS_ROOT`. Подтверждено devops-reviewer; qa-гард совпадения env-имён с полями `Settings` на месте ([06-testing-strategy.md](06-testing-strategy.md)). | Sprint 1 | **CLOSED в Sprint 1.** Compose/`.env.example` соответствуют каноническому контракту имён символ-в-символ. |
| <a id="td-004"></a>TD-004 ✅ **CLOSED (Sprint 3)** | **[ПОГАШЕН]** ~~Auth: O(N) перебор всех юзеров с argon2-verify на каждый запрос.~~ В Sprint 3 backend реализовал индексируемый O(1) lookup токена ([ADR-008](adr/ADR-008-indexed-api-key-lookup.md)): opaque-ключ несёт `key_id`-префикс (`lv_<key_id>_<secret>`), UNIQUE-индекс `api_tokens.key_id` → O(1) поиск одной строки + один constant-time argon2-verify; full-scan по всем юзерам убран. Покрыто тестом, что верификация не зависит от числа юзеров. Связано с [05-security.md → Аутентификация](05-security.md#аутентификация). Подтверждено финальным reviewer (`production_ready: true`, qa 329 passed / coverage 87.26%). | Sprint 1 (введён), решение Sprint 3 | **CLOSED в Sprint 3.** Индексируемый lookup ([ADR-008](adr/ADR-008-indexed-api-key-lookup.md)) реализован; продуктовое решение ([08-product-decisions.md §3-3](08-product-decisions.md#sprint-3--auth--multi-user)) выполнено. |
| <a id="td-003"></a>TD-003 ✅ **CLOSED (Sprint 4)** | **[ПОГАШЕН]** ~~Дельта teardown-on-fail (S1) ↔ GC-on-project-delete (S4): не покрыт GC orphaned-ресурсов при удалении проекта.~~ В Sprint 4 backend+devops реализовали `DELETE /projects/{pid}`→`202`+soft-delete (`projects.deleted_at`) + асинхронный Celery `project.gc` ([ADR-011](adr/ADR-011-project-delete-gc.md)): полный teardown всех контейнеров/Traefik-route проекта, volume, S3-артефактов всех ревизий/деплоев, БД-каскад; in-flight джобы → `FAILED(project_deleted)`; идемпотентно; субдомены opaque (`[a-z0-9]{16}`) и **не реюзаются** (защита от subdomain-takeover). Нормативный контракт — [modules/deploy/03-architecture.md §6](modules/deploy/03-architecture.md#6-gc-при-удалении-проекта-sprint-4--delete-projectsid-adr-011-закрывает-td-003q-deploy-3), [modules/api/02-api-contracts.md → DELETE](modules/api/02-api-contracts.md#delete-projectspid-sprint-4). Подтверждено финальным reviewer Sprint 4 (`production_ready: true`, qa 462 passed / coverage 87.81%). | Sprint 1 (введён), решение Sprint 4 | **CLOSED в Sprint 4.** `DELETE /projects` + `project.gc` ([ADR-011](adr/ADR-011-project-delete-gc.md)) реализованы и покрыты тестами; [Q-DEPLOY-3](99-open-questions.md#q-deploy-3) resolved. **Открыт остаточный наблюдаемый эффект:** eventual-окно между `202` и завершением async `project.gc` — заведён [TD-010](#td-010) (метрика/алерт на gc-lag → S6). |
| <a id="td-005"></a>TD-005 ✅ **CLOSED-for-S6 (метрика-драйвер реализован, тонкая калибровка — пост-релиз)** | **[МЕТРИКА-ДРАЙВЕР РЕАЛИЗОВАН]** ~~Калибровка нормализаторов сигнатуры фейла + версионирование `failure_log` по витку (S2).~~ No-progress detection ([ADR-005](adr/ADR-005-no-progress-failure-signature.md)) использует фиксированный список нормализаторов (пути/таймстампы/PID/hex/`{job_id}`) — он неполон до накопления реальных build/health-логов, возможны редкие ложно-положительные/-отрицательные срабатывания (обрыв на попытку раньше / откладывание до hard cap — оба безопасны, не runaway). Дополнительно: `failure_log` (`logs/{job_id}/build.log`) **перезаписывается** каждым витком FIXING (Agent 4 нужен лог последней попытки); per-attempt история сейчас только в `job_events`, не в S3. | Sprint 2 | **CLOSED-for-S6.** В Sprint 6 реализован и покрыт тестами **метрика-драйвер калибровки** ([observability §5.1](modules/observability/03-architecture.md#51-нормализаторы-сигнатуры-фейла-td-005)): метрика `lovable_no_progress_trips_total` (частота no_progress vs `lovable_job_failed_total{reason="no_progress"}`) инструментирована — даёт сигнал для калибровки списка нормализаторов на реальных логах. **Остаток (пост-релиз, не пробел):** тонкая калибровка значений нормализаторов выполняется **по дашборду Cost/no_progress после накопления боевых данных** — требует живого трафика, не блокирует релиз. **Версионирование лога по `retry_count` — РЕАЛИЗОВАНО ([ADR-022](adr/ADR-022-per-attempt-build-logs.md), пост-S6 фикс прод-инцидента):** per-attempt ключи `logs/{job_id}/build.{retry_count}.log` / `deploy.{retry_count}.log` / `agent.{retry_count}.log` (три раздельных имени-стадии при одном `retry_count`; `agent.{n}` — отклонённый патч Agent 4, не инкрементирующий `retry_count`; лог попытки **не** затирается пересборкой после фикса Agent 4) → post-mortem ранних витков возможен по их собственным логам. `generation_jobs.failure_log_ref` по-прежнему указывает на лог **последней** попытки (вход Agent 4); нормативно — [pipeline §F](modules/pipeline/03-architecture.md#f-failure_log-в-s3), [deploy §F-1](modules/deploy/03-architecture.md#f-1-per-attempt-ключи-builddeploy-логов-adr-022). Подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). Cross-ref [ADR-015](adr/ADR-015-observability-stack.md), [ADR-022](adr/ADR-022-per-attempt-build-logs.md), [Q-COST-1](99-open-questions.md#q-cost-1). |
| <a id="td-006"></a>TD-006 ✅ **CLOSED-for-S6 (Redis budget-кэш реализован, профиль латентности — пост-релиз)** | **[РЕАЛИЗОВАН]** ~~Быстрый Redis-счётчик бюджета не реализован (оптимизация, не пробел).~~ Budget-гард fix-loop ([modules/pipeline/03-architecture.md → §C(b)](modules/pipeline/03-architecture.md#c-четыре-гарда-от-бесконечного-цикла-и-runaway-затрат)) читал `generation_jobs.spend_usd` **из Postgres** — Postgres является источником истины бюджета, гард функционально корректен и авторитетен для DoD Sprint 2. Redis-счётчик бюджета как «быстрый гейт» в реализации S2 не был заведён — недостающая оптимизация латентности гейта при масштабе. Согласовано с остатком [Q-COST-1](99-open-questions.md#q-cost-1) и [TD-005](#td-005). | Sprint 2 | **CLOSED-for-S6.** В Sprint 6 реализован и покрыт тестами **Redis budget-кэш** ([observability §5.2](modules/observability/03-architecture.md#52-redis-budget-счётчик-td-006--опциональная-оптимизация-латентности-гейта)): счётчик `budget:{job_id}` (`INCRBYFLOAT` после записи `llm_usage`, TTL=`JOB_WALL_CLOCK_BUDGET_S`) как read-through кэш-гейт; **cache-miss → fallback на Postgres `spend_usd` + пере-засев ключа** (гейт никогда не пропускается без Redis-ключа); Postgres остаётся источником истины ([pipeline §C(b)](modules/pipeline/03-architecture.md#b-cost-cap-budget_usd)). **Остаток (пост-релиз, не пробел):** подтверждение выигрыша латентности по дашборду Cost на боевом масштабе — требует живого трафика, не блокирует релиз. Подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). Cross-ref [ADR-015](adr/ADR-015-observability-stack.md), [ADR-016](adr/ADR-016-scale-topology-redis-pool.md). |
| <a id="td-001"></a>TD-001 ✅ **CLOSED (Sprint 4)** | **[ПОГАШЕН]** ~~Сборка через смонтированный `docker.sock` без gVisor/rootless и без egress-allowlist (S1) → эскалация до хоста при компрометации воркера.~~ В Sprint 4 backend+devops реализовали **rootless Docker** ([ADR-010](adr/ADR-010-build-sandbox-rootless-egress.md)): build-воркер на rootless-сокете (`BUILD_SANDBOX_RUNTIME=rootless`) вместо привилегированного `docker.sock`; build-контейнер недоверенного кода изолирован — `cap-drop ALL`, `no-new-privileges`, seccomp, `--read-only` rootfs кроме `/workspace`, non-root UID, лимиты `BUILD_*` (`--cpus`/`--memory`/`--pids-limit`/wall-clock). **Egress-allowlist:** build-контейнер в изолированной `BUILD_EGRESS_NETWORK` + egress-proxy, пропускающий **только** `NPM_REGISTRY_ALLOWLIST`; транспорт через `BUILD_EGRESS_PROXY_URL` (воркер инжектит `http_proxy`/`https_proxy`), private CIDR/cloud-metadata — DROP. Нормативная конфигурация — [05-security.md → «Конфигурация запуска build-контейнера»](05-security.md#конфигурация-запуска-build-контейнера-sprint-4-нормативная), [modules/deploy/03-architecture.md §1 → Sprint 4](modules/deploy/03-architecture.md#1-sandbox-исполнение-недоверенного-кода). Подтверждено финальным reviewer Sprint 4 (`production_ready: true`, qa 462 passed / coverage 87.81%). | Sprint 1 (введён), решение Sprint 4 | **CLOSED в Sprint 4.** Rootless Docker + egress-allowlist ([ADR-010](adr/ADR-010-build-sandbox-rootless-egress.md)) реализованы и покрыты unit/integration-тестами; [Q-INFRA-1](99-open-questions.md#q-infra-1)/[Q-DEPLOY-1](99-open-questions.md#q-deploy-1) resolved. **Открытый приёмочный пункт (не реоткрывает TD):** живой энфорс rootless+egress+npm-через-proxy на реальном build-хосте (rootless Docker-демон + egress-proxy) НЕ прогонялся — 2 real-stack теста skip; зафиксирован как приёмочный пункт Sprint 5 (см. [README → Статус Sprint 4](README.md#статус-sprint-4-реализовано)). |
| <a id="td-007"></a>TD-007 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН]** ~~Redis-подключение без пула в rate-limit (hot path, S3).~~ `app/auth/rate_limit.py` создавал новое подключение к Redis на каждый аутентифицированный запрос (`aioredis.from_url(...)` + `aclose()` per-request, hot path). В Sprint 6 backend реализовал переиспользуемый единый `ConnectionPool` / клиент-синглтон на процесс (web/worker) взамен per-request `from_url`/`aclose` во всех hot-path-точках (`rate_limit`/SSE/budget) ([ADR-016](adr/ADR-016-scale-topology-redis-pool.md)); параметры — env `REDIS_POOL_MAX_CONNECTIONS`/`REDIS_POOL_TIMEOUT_S` ([07-deployment](07-deployment.md#канонический-список-ключей)). Наблюдаемость — `lovable_redis_pool_in_use{pool}` ([observability §2.6](modules/observability/03-architecture.md#26-queue--worker-scale)). Покрыто тестами, подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 3 (введён), решение Sprint 6 | **CLOSED в Sprint 6.** Redis `ConnectionPool` во всех hot-path ([ADR-016](adr/ADR-016-scale-topology-redis-pool.md)) реализован и покрыт тестами. |
| <a id="td-009"></a>TD-009 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН]** ~~`run_periodic_resync` без батча/LIMIT (S3.5).~~ Celery-beat `billing.resync` ([ADR-009 §B](adr/ADR-009-billing-idempotency-resync-grace.md)) выбирал пользователей с протухшим `subscriptions.synced_at` / `status ∈ {grace, billing_issue}` без `.limit(BATCH)` и без курсора — при росте числа протухших подписок один beat-тик деградировал в длинную последовательную серию `getProfile`-вызовов. В Sprint 6 backend реализовал `.limit(BILLING_RESYNC_BATCH_SIZE)` + курсор `synced_at ASC` (самые протухшие первыми, хвост на последующих тиках) ([billing §3.1](modules/billing/03-architecture.md#31-периодический-celery-beat-billingresync)). Наблюдаемость — `lovable_billing_resync_batch` + `lovable_adapty_resync_lag_seconds` + Grafana-alert при lag > `2×BILLING_RESYNC_INTERVAL_S` ([observability §2.6/2.7](modules/observability/03-architecture.md#26-queue--worker-scale)). Покрыто тестами, подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 3.5 (введён), решение Sprint 6 | **CLOSED в Sprint 6.** Batch+курсор resync ([ADR-016](adr/ADR-016-scale-topology-redis-pool.md), [ADR-009](adr/ADR-009-billing-idempotency-resync-grace.md)) реализован и покрыт тестами. |
| <a id="td-010"></a>TD-010 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН]** ~~Eventual-окно async `project.gc` без метрики/алерта (S4).~~ Между `202` от `DELETE /projects/{pid}` и завершением async Celery `project.gc` ([ADR-011](adr/ADR-011-project-delete-gc.md)) существовало наблюдаемое окно без метрики/алерта на застревание. В Sprint 6 backend+devops реализовали метрики `lovable_project_gc_pending` (gauge, soft-deleted с незавершённым GC) / `lovable_project_gc_duration_seconds` (histogram) + Grafana-alert на gc-lag выше порога ([observability §2.5](modules/observability/03-architecture.md#25-edit--rollback--deploy-deploy), [§3](modules/observability/03-architecture.md#3-grafana-дашборды)). Покрыто тестами, подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 4 (введён), решение Sprint 6 | **CLOSED в Sprint 6.** Метрика/алерт gc-lag ([ADR-015](adr/ADR-015-observability-stack.md)) реализованы и покрыты тестами. Cross-ref [TD-003](#td-003) (closed S4), [ADR-011](adr/ADR-011-project-delete-gc.md). |
| <a id="td-008"></a>TD-008 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН]** ~~N+1 в `list_projects` (live_url в цикле, pre-existing S1).~~ `app/api/routers/projects.py::list_projects` вызывал `get_project_live_url(...)` в цикле по всем проектам (N+1, pre-existing с S1, выявлено reviewer S3, severity minor). В Sprint 6 backend реализовал batched live_url — один запрос на список деплоев активных проектов (`WHERE project_id IN (...)`) вместо запроса на проект ([ADR-016](adr/ADR-016-scale-topology-redis-pool.md)). Проверка — integration-тест: N проектов → 1 SQL-запрос ([06-testing §S6](06-testing-strategy.md), [observability §6](modules/observability/03-architecture.md#6-scale-наблюдаемость-и-закрытие-долга-cross-ref-adr-016)). Подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 1 (введён), выявлено S3, решение Sprint 6 | **CLOSED в Sprint 6.** Batched live_url ([ADR-016](adr/ADR-016-scale-topology-redis-pool.md)) реализован и покрыт integration-тестом (N проектов → 1 SQL). |
| <a id="td-011"></a>TD-011 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН]** ~~SSE heartbeat-ветка не дочитывает `job_events` по таймауту (S5).~~ `app/api/sse.py`: heartbeat-ветка слала keepalive-ping, но не до-читала `job_events` по таймауту (doc↔code mismatch + риск «висящего» стрима при потерянном pub/sub терминального события). В Sprint 6 backend привёл код к контракту: по heartbeat-таймауту до-читать `job_events WHERE job_id=:jid AND id > :last_seen_id ORDER BY id`, отдать хвост и закрыть стрим `event: done` при терминальном событии (страхует от потерянного pub/sub без reconnect). Наблюдаемость — `lovable_sse_heartbeat_catchup_total{result}` ([observability §2.3](modules/observability/03-architecture.md#23-sse--realtime-api)). Покрыто тестами, подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 5 (введён), решение Sprint 6 | **CLOSED в Sprint 6.** SSE heartbeat re-read `job_events` + `done` на терминале реализован и покрыт тестами. Cross-ref [ADR-012](adr/ADR-012-sse-realtime-transport.md), [ADR-015](adr/ADR-015-observability-stack.md). |
| <a id="td-013"></a>TD-013 ⏳ **OPEN (оптимизация латентности, не блокер)** | **Inherent латентность Agent 3 (Builder) ~3 мин на дерево файлов.** Генерация полного файл-дерева Agent 3 занимает ~3 мин; лишний fix-loop (исторически провоцируемый `vite: not found`/потерей `--base`) удваивал это окно — устранено инфра-фиксом [ADR-017 §Fix (2026-06-08)](adr/ADR-017-path-based-site-routing.md#fix-2026-06-08--vite-not-found--потеря---base-через-npm-run-build-прод-инцидент) (нормализация к `npx vite build`). **Остаточная** базовая латентность Agent 3 — inherent LLM-стоимость, не дефект. **Будущая оптимизация:** prompt caching стабильных system-промтов между агентами/fix-итерациями (Anthropic SDK, уже отмечено в [pipeline §token-budget](modules/pipeline/03-architecture.md)). | пост-S6 (выявлено при прод-фиксе 2026-06-08) | **OPEN, НЕ блокер фикса ADR-017 §Fix.** Погашение — отдельной задачей оптимизации (prompt caching), при наличии сигнала по дашборду Cost/латентности. Cross-ref [ADR-023](adr/ADR-023-agent3-token-budget-thinking-room.md), [ADR-017 §Fix (2026-06-08)](adr/ADR-017-path-based-site-routing.md#fix-2026-06-08--vite-not-found--потеря---base-через-npm-run-build-прод-инцидент). |
| <a id="td-012"></a>TD-012 ✅ **CLOSED (Sprint 6)** | **[ПОГАШЕН наблюдаемостью]** ~~`kind=rollback`/`edit` занимают слот `max_concurrent_jobs` (S5).~~ `active_jobs(user)` = `COUNT` нетерминальных `generation_jobs` без фильтра по `kind` — in-flight `edit`/`rollback` занимает слот concurrency наравне с `generation` (на Free `max_concurrent_jobs=1` блокирует старт новой generation/edit). Поведение **закреплено нормативно** ([billing §4.3](modules/billing/03-architecture.md#4-entitlements--quota-gate)) — документированное поведение тарифа, **не баг**; долг был в наблюдаемости. В Sprint 6 backend реализовал метрику `lovable_concurrency_block_by_kind_total{blocked_kind,holder_kind}` ([observability §2.7](modules/observability/03-architecture.md#27-billing--quota-billing), [§6](modules/observability/03-architecture.md#6-scale-наблюдаемость-и-закрытие-долга-cross-ref-adr-016)). **Продуктовое решение S6: rollback/edit ОСТАЮТСЯ в cap** (семантика слота не меняется), долг закрыт метрикой. Покрыто тестами, подтверждено финальным reviewer S6 (`production_ready: true`, qa 604 passed / coverage 83.59%). | Sprint 5 (введён), решение Sprint 6 | **CLOSED в Sprint 6.** Метрика concurrency-block реализована; rollback/edit остаются в cap (нормативно [billing §4.3](modules/billing/03-architecture.md#4-entitlements--quota-gate)). **Пересмотр** (вынести rollback из cap) — отдельным ADR при существенной доле блокировок на дашборде Billing, не в S6. Cross-ref [ADR-014](adr/ADR-014-edit-limit-revision-rollback.md), [ADR-015](adr/ADR-015-observability-stack.md). |
